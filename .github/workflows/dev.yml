name: Deploy to DEV
on:
  push:
    branches:
      - dev
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v3

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: sn471259.ftp.tools
          username: sn471259_kostiantyn
          password: m2ES4vXp2vJ4
          server-dir: /web-dev.stream/www/slots/
